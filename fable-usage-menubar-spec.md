# Fable 使用量メニューバー表示 — 開発指示書

## 0. これは何か

Claude Code の週間使用量、特に **Fable のモデル別制限** を macOS のメニューバーに常時表示する。
既存の ClaudeMeter は 5時間枠と週間(全モデル)しか表示できず、Fable の枠を取得できないことが確認済み。

### 確認済みの事実

`claude-meter --json` の出力(2026-08-29 時点):

```json
"usage": {
  "five_hour":        { "utilization": 4.0, "resets_at": "..." },
  "seven_day":        { "utilization": 9.0, "resets_at": "..." },
  "seven_day_sonnet": null,
  "seven_day_opus":   null,
  "seven_day_oauth_apps": null,
  "seven_day_omelette":   null,
  "seven_day_cowork":     null
}
```

モデル別フィールドは存在するが全て `null`。**Fable に相当するキーがそもそもない。**
一方、claude.ai の設定画面と Claude Code の `/usage` には Fable の数値が表示されている。

→ つまりデータは存在するが、claude-meter が見ているエンドポイントが返していない。

---

## 1. 要件

### 必須

- メニューバーに Fable の週間使用率(%)を常時表示する
- 5時間枠・週間(全モデル)も併記できる
- **アイドル時のメモリ消費を最小化する**
- **推論トークンを消費しない**(詳細は §4)
- 取得失敗時に古い値を「正しい現在値」として表示しない

### 非目標

- 他人への配布(署名・公証・サンドボックスの考慮を全て捨てられる)
- コスト($)の集計 — ccusage 系の領分であり今回の目的ではない
- Windows / Linux 対応

---

## 2. アーキテクチャ

**取得層と表示層を分離すること。** これが設計上もっとも重要な判断。

```
[取得層] launchd で定期実行するスクリプト
   ↓ 書き出し
[キャッシュ] ~/.cache/fable-meter/state.json
   ↓ 読むだけ
[表示層] SwiftBar / xbar プラグイン(数十msで終わる)
```

### なぜ分けるのか

- 表示層は数秒おきに走るが、キャッシュを読むだけなので常に軽い
- 取得層が数秒かかっても、遅いのが表示に伝播しない
- 取得層が失敗しても、表示層は「古い値である」ことを明示して出せる
- 取得方式(経路A / B)を差し替えても表示層は無変更で済む

### 表示層に SwiftBar を使う理由

自前で SwiftUI の `MenuBarExtra` アプリを書いてもいいが、配布しないなら SwiftBar にスクリプトを置くだけで済む。
署名・公証・Gatekeeper の問題が**まるごと消える**(手元の環境で署名まわりの問題に当たった経緯があるため、この点は重要)。

```
brew install --cask swiftbar
```

プラグインは `~/Library/Application Support/SwiftBar/Plugins/` に置く。
ファイル名の `fable.10s.sh` の `10s` が実行間隔。キャッシュを読むだけなので短くて問題ない。

---

## 3. データ取得 — 経路A と 経路B

**まず経路A を調査すること。** ダメなら経路B に落とす。経路B は確実に動くが実装が重い。

### 経路A: claude.ai のエンドポイントを直接叩く(第一候補)

#### 調査手順

1. ブラウザで claude.ai の設定 → 使用量 のページを開く
2. 開発者ツール → Network タブ → Fetch/XHR でフィルタ
3. ページをリロード
4. `usage` / `limits` / `organizations` を含むリクエストを探す
5. 各レスポンスを開き、**Fable の数値(設定画面と一致する%)が含まれるものを特定する**
6. 見つかったら、そのリクエストの URL・メソッド・必要なヘッダを記録する

既知の値: `org_uuid`(個人識別子のためリポジトリには記載しない。調査の結果 org_uuid 不要のエンドポイントで取得できたので不要)
→ URL にこの UUID が含まれる可能性が高い。

#### 認証

Claude Code の認証情報は Keychain の `Claude Code-credentials` に入っている。
これを読めば `Authorization` ヘッダを組める。

```bash
security find-generic-password -s "Claude Code-credentials" -w
```

初回に許可ダイアログが出る。「常に許可」を選ぶと以降は聞かれない。

#### 経路A の判定

- レスポンスに Fable の数値がある → **経路A で進む**。実装はレスポンスをパースするだけ
- ない、または設定画面が別経路で数値を得ている → **経路B に切り替える**

### 経路B: `/usage` の出力をパースする(確実だが重い)

Claude Code 本体が表示している内容なので、Fable は**必ず含まれる**。

#### 方式

疑似ターミナル(PTY)で `claude` を起動し、`/usage` を送り、出力をキャプチャする。

```
1. PTY で claude を spawn
2. 起動バナー(Claude Code v\d+ 等)を待つ
3. "/usage\n" を送る
4. 出力が落ち着くまで待つ
5. ANSI エスケープシーケンスを除去
6. "Fable" を含む行から数値を抽出
7. キャッシュに書き出してプロセスを終了する
```

#### 実装上の注意

- **完璧な ANSI パーサは不要。** 配布しないので、エスケープを正規表現で剥がして数字を拾えば十分。CC Usage Bar が仮想スクリーンを再構築しているのは、あらゆる環境で色付き表示を再現するためであり、今回の目的には過剰
- 起動からレスポンスまで数秒かかる。**必ずタイムアウト(20〜30秒)を設ける**
- 終了処理を確実に行う。ゾンビプロセスが溜まると常駐している意味がなくなる
- `/usage` の表示レイアウトが変わるとパースが壊れる。壊れたときに古い値を出さないこと(§5)

---

## 4. トークンとメモリを使わない設計

### トークン

- **`/usage` はローカルのスラッシュコマンドであり、モデルへの推論リクエストを発生させない。** 使用量そのものを増やす心配は基本的に不要
- ただし念のため、実装後に一度**手動で検証すること**: 実行前後で `/usage` の数値が変わらないかを確認する。変わるなら経路A に切り替える
- 絶対にやってはいけないこと: 使用量を調べるために通常のプロンプトを投げる。本末転倒になる

### メモリ

| 方式 | 常駐メモリ | 評価 |
|---|---|---|
| Electron アプリ | 150〜300MB | 却下 |
| Swift 常駐アプリ | 20〜30MB | 可。ただし自作コストが乗る |
| SwiftBar + スクリプト | SwiftBar 本体のみ(~30MB) | **推奨** |
| tmux / starship への埋め込み | ほぼゼロ | 最軽量。ターミナル常駐者向け |

### 取得層を常駐させないこと

取得スクリプトは **launchd で起動 → 実行 → 終了** させる。デーモンとして常駐させない。
アイドル時のメモリ消費が文字通りゼロになる。

`~/Library/LaunchAgents/com.local.fable-meter.plist`:

```xml
<key>StartInterval</key>
<integer>300</integer>
<key>RunAtLoad</key>
<true/>
```

### ポーリング間隔

- 経路A: 5分間隔で十分。負荷は無視できる
- 経路B: 5〜10分間隔。claude の起動コストがあるため短くしない
- どちらも**スリープ復帰直後は1回スキップ**する(ネットワーク未接続で無駄に失敗するため)

---

## 5. 想定される問題と対策

### 致命的なもの

| 問題 | 影響 | 対策 |
|---|---|---|
| 古い値を現在値として表示 | **最悪。制限に達していないと誤認して作業計画を誤る** | キャッシュに `fetched_at` を必ず記録。表示層で5分以上古い場合は `4%?` のように不確実マークを付ける。30分以上なら `--` にする |
| 非公開APIの仕様変更(経路A) | ある日突然取れなくなる | 失敗を握り潰さない。エラー時は明示的に `!` を表示。ログに残す |
| `/usage` の表示変更(経路B) | パースが静かに壊れる | 数値が取れなかった場合と `0%` を厳密に区別する。パース失敗は必ずエラー扱い |

### 運用上のもの

| 問題 | 対策 |
|---|---|
| 認証トークンの漏洩 | **絶対にログやキャッシュに書き出さない。** デバッグ出力にも含めない |
| Keychain 許可を「許可」で押してしまった | 「常に許可」でないと毎回ダイアログが出る。キーチェーンアクセスからアクセス制御を修正 |
| ゾンビプロセスの蓄積(経路B) | タイムアウト後に確実に kill。`pgrep` で残存を確認する処理を入れる |
| Fable のキー名が将来変わる | `seven_day_omelette` のような内部コード名が使われている前例がある。キーは決め打ちにせず、見つからない場合はエラーとして扱う |
| 週間制限のプロモーション期間 | 2026年8月31日まで Claude Code の週間制限が50%引き上げられている。**9月1日以降は同じ作業量でも%が跳ね上がる。** 閾値アラートを設定する場合、この日を跨いだ挙動変化を異常と誤認しないこと |
| レート制限 | 5分間隔なら問題ないが、デバッグ中に連打しないこと |

---

## 6. 表示仕様

### メニューバー

```
Fable 11% · 7d 9% · 5h 4%
```

Fable を先頭に置く。これが一番見たい数字であり、実測で全モデル枠(9%)より速く消費されている。

### 状態による変化

| 条件 | 表示 |
|---|---|
| 正常 | `Fable 11% · 7d 9% · 5h 4%` |
| Fable が 80% 超 | 背景を黄色に |
| Fable が 95% 超 | 背景を赤に |
| データが5分以上古い | 数値の後ろに `?` |
| データが30分以上古い / 取得失敗 | `Fable --` とし、ドロップダウンにエラー内容 |

### ドロップダウン

- 各枠の数値とリセット時刻
- 最終取得時刻
- `Refresh now`(取得スクリプトを手動実行)
- エラーがあればその内容

---

## 7. 進め方

1. **経路A の調査を最初にやる。** 開発者ツールで Fable を含むレスポンスを探す。ここが分岐点であり、30分で判明する
2. 取得スクリプトを書く。まず標準出力に JSON を吐くだけのものを作り、単体で正しい値が出ることを確認する
3. キャッシュへの書き出しと launchd の設定を追加する
4. SwiftBar プラグインを書く。キャッシュを読んで1行出すだけ
5. 異常系を作り込む。**ここを飛ばすと §5 の「古い値を表示」を踏む**
6. トークン消費が発生しないことを実測で確認する

### 経路A が使えた場合の副産物

「このエンドポイントなら Fable が取れる」という情報は、ClaudeMeter 側の issue としてそのまま出せる。
本家が対応すれば自作分は捨てられるので、調査した労力は無駄にならない。
